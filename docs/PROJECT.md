# Backsignal: план и эксплуатация

Короткий живой документ. Обновлять после завершения этапов и важных изменений инфраструктуры.

## Статус

### 0. Фундамент — завершено

- [x] Neon создан, URL добавлены локально и в Vercel
- [x] Prisma 7.10 + Neon adapter настроены
- [x] Соединение с Neon проверено запросом
- [x] Мультитенантная схема создана
- [x] Первая миграция применена
- [x] DAL и проверки доступа созданы
- [x] `.env.example` добавлен
- [x] Постоянный домен QR: `https://backsignal.tech/q/{publicId}`
- [x] Neon разделён на ветки `production` и `stage`
- [x] Создан отдельный Vercel-проект для ветки `stage`
- [x] Перекрёстные Preview-сборки отключены
- Dockerfile и деплой на VPS отложены до этапа переноса; не блокируют фундамент MVP.

### 1. Интернационализация — завершено

- [x] Подключён `next-intl`
- [x] Добавлены локали `ru` и `en`
- [x] Строки лендинга и metadata перенесены в JSON-словари
- [x] Настроен locale routing: `/` для `ru`, `/en` для `en`
- [x] Добавлены типизированные locale-aware Link, router и pathname
- [x] Добавлен переключатель языка: desktop Popover и mobile Drawer на Base UI
- [x] Языковые предпочтения сохраняются через cookie `next-intl`

### 2. Авторизация — базовый flow завершён

- [x] Better Auth + Prisma
- [x] Email + password: регистрация, вход и выход
- [x] Сессии и защита тестового кабинета
- [x] Подтверждение email и восстановление пароля
- [x] Локализованные страницы и письма для `ru` и `en`
- [x] Resend подключён через сменяемый почтовый адаптер
- [x] Yandex ID через Generic OAuth и объединение с подтверждённым локальным аккаунтом при совпадении email
- [x] Пользователь может состоять в нескольких организациях
- Управление способами входа (подключение/отключение Yandex) — позже в настройках профиля

### 3. Основа кабинета — реализовано

- [x] Защищённый dashboard layout
- [x] Desktop aside и мобильная навигация на Base UI Dialog
- [x] Header и главное окно с обычным скроллом страницы
- [x] Разделы «Сообщения» и «Настройки», отдельная страница профиля
- [x] Базовые loading/error/empty состояния; демонстрационные сообщения без записи в БД
- [x] Пользователь, язык и выход в нижней части aside; большого footer нет
- Название, ссылка и QR доступны после onboarding; реальные сообщения подключены через DAL, карточки со смайликами, дата UTC, пагинация по 30 и ручное обновление. Непрочитанные подсвечены; открытие сохраняет READ/readAt и обновляет общий счётчик. Редактирование настроек пока не подключено.
- [x] Пользователь проверил меню профиля на desktop и mobile: dropdown, шторка профиля, выбор языка, закрытие по тапу вне шторки
- Клавиатурную навигацию проверить вручную
- Переключатели организации и объекта пропускаются в MVP; добавить при разработке платной версии (Free: одна организация)
- Для платной версии с несколькими организациями/объектами добавить обозначение источника в карточке сообщения; в Free название объекта в каждой карточке скрыто.

### 4. Обязательный онбординг в кабинете — завершено

- [x] Серверная проверка доступной организации при открытии dashboard
- [x] Если организации нет — блокирующий Dialog на Base UI поверх кабинета
- [x] Реализованы DAL и Server Action: транзакция с блокировкой строки пользователя защищает от двойного создания; Free — одна организация. Схема БД не изменена, миграция не нужна.
- [x] Ссылка на собственном домене `/q/[publicId]`, QR в PNG и копирование доступны после создания и в настройках. Публичную форму проверить после применения миграции этапа 5.
- [x] Пользователь проверил онбординг после исправлений; основной сценарий работает корректно.
- Дополнительные регрессионные сценарии: двойная отправка из двух вкладок, RU/EN, mobile scroll и блокировка Escape/backdrop.
- [x] Диалог нельзя закрыть по Escape, клику по фону или крестику
- [x] Первоначально одно поле: название места, проекта или бизнеса
- [x] Одной транзакцией создать `Organization`, `Membership` с ролью `OWNER`, первый `FeedbackTarget` и первый `FeedbackChannel`
- [x] Для MVP использовать введённое название для организации и первого объекта
- [x] После создания обновить dashboard и показать пустой раздел «Сообщения»
- [x] Ссылку и QR показать после завершения либо в настройках объекта
- [x] Не хранить отдельный `onboardingCompleted`: источник истины — наличие доступной организации и первого объекта

### 5. Публичная форма — реализовано, ожидает полной проверки на stage

- [x] `/q/[publicId]` без locale prefix, отдельный public root layout, исключение `/q` из next-intl proxy
- [x] Название объекта и локализованная форма RU/EN, noindex, недоступный/отключённый канал
- [x] Mobile-first по референсу: логотип, название объекта, заголовок, textarea, три смайлика, отправка, «Без регистрации». На desktop — компактная форма по центру, без dashboard-навигации.
- [x] Настроение сообщения: `POSITIVE`, `NEUTRAL`, `NEGATIVE`; довольный / нейтральный / недовольный. Без выбора сервер сохраняет `NEUTRAL`. Выбор выделяется; доступная radio-группа. Enum/поле Message подготовлены в схеме и миграции (существующие сообщения — нейтральные после миграции).
- [x] Защита MVP: серверная Zod-валидация, honeypot, атомарный rate limit в PostgreSQL, HMAC-IP, идентификатор отправки от дублей. Лимиты стартовые: 30 попыток/IP/10 минут и 3 сообщения/IP/канал/10 минут; учитывать общий Wi-Fi.
- CAPTCHA пока не подключать — решение пользователя. При появлении ботов выбрать провайдера и добавить обязательную серверную проверку токена.
- [x] Server Action сохраняет сообщение; экран благодарности, сохранение текста при ошибке, защита повторной отправки
- [x] Язык: cookie → язык объекта → язык браузера → `ru`; ручной RU/EN устанавливает NEXT_LOCALE без смены QR URL
- [x] Пользователь применил миграцию `20260917090000_public_feedback`; сообщения публичной формы сохраняются в БД
- [x] Подключены реальные сообщения к dashboard через DAL; доступ только к своей организации
- [ ] Проверить лимиты, повторные/параллельные отправки, отключённые каналы, мобильный вид, RU/EN и отображение в dashboard
- Rate limit: общий PostgreSQL fixed window 10 минут; истёкшие записи удаляются ограниченными порциями при отправках (для строгого срока хранения позже добавить cron). IP доверяется только заголовку Vercel; локально общий localhost. Для VPS настроить доверенный reverse proxy до запуска формы. Опциональный PUBLIC_RATE_LIMIT_SECRET, fallback BETTER_AUTH_SECRET.

### 6. QR-конструктор — в работе

- [x] Aside: Сообщения → Создать макет (`/dashboard/qr`) → Только QR-код (`/dashboard/qr-code`) → Настройки. Обычный QR также остаётся в настройках/онбординге; канал выбирается через серверный DAL текущего пользователя
- [x] Обычный QR: SVG и PNG 2048 px
- [x] PDF через `@react-pdf/renderer`, генерация по кнопке в браузере, lazy import; Chromium/API/хранение файлов не нужны
- [x] Листовки A4/A5/A6/105×210 мм; отдельная горизонтальная карточка-вкладыш 90×50 мм
- [x] Название и призыв, язык RU/EN независимо от языка кабинета; настройки не сохраняются
- [x] Векторный QR с quiet zone 4 модуля; локальный статический Inter Regular/Bold с OFL-лицензией
- [x] Предпросмотр настоящего PDF на десктопе, открыть PDF на мобильном, скачивание; после изменения настроек требуется обновить предпросмотр
- [ ] Проверить экспорт на stage, кириллицу/длинные тексты, реальные размеры/печать и сканирование, Android/iPhone
- [x] Пользователь проверил текущий конструктор; общий вид/работа устраивают. Физическая печать, сборка и полный mobile/stage-прогон отдельно не подтверждены.
- Позднее: раскладка нескольких карточек на A4, вылеты/метки реза, сохранение настроек
- Уточнение: бренд заменён векторным RU/EN логотипом из assets; селекты конструктора используют общий `ui/select.tsx` (Base UI). PDF-шаблон: `components/dashboard/qr-material-document.tsx`, векторные логотипы: `qr-print-logo.tsx` (обновлять после изменения исходных SVG).
- [x] «Пирамидка»: развёртка A4 альбомная 297×210 мм, три грани 90×190 мм, клапан 10 мм, внешний контур реза/пунктир сгибов, одинаковые RU/EN логотип + название + призыв + QR на каждой грани. Проверить физическую сборку/печать пользователем.
- VPS: при усложнении шаблонов рассмотреть HTML/CSS → PDF через Playwright + Chromium; необязательная замена текущего генератора
- Проверки реализации: TypeScript/ESLint; `node scripts/qr-materials.test.mjs` — 14 тестов: вектор/quiet zone, 6 форматов × RU/EN с кириллицей/точными MediaBox и сохранение первой буквы английского призыва (извлечение текста через системный `pdftotext`). Не заменяет визуальную проверку, печать и сканирование.

### 7. Уведомления — реализованы, базовая отправка проверена

- [x] Почтовый адаптер с Resend и возможностью смены провайдера
- [x] Подтверждение регистрации и восстановление пароля
- [x] Новые отзывы: RU/EN письмо владельцу с подтверждённым email, фрагмент до 300 символов, настроение, ссылка на конкретное сообщение/настройки; HTML экранируется
- [x] Отдельный отправитель `NOTIFICATION_EMAIL_FROM` на `notify.backsignal.tech` (пользователь подтвердил DNS/Resend); auth остаётся на `auth.backsignal.tech`
- [x] Очередь PostgreSQL в транзакции отзыва; первая отправка через `after()` после ответа, без сетевого запроса внутри транзакции. Honeypot/повтор submissionId не создают писем
- [x] Уникальность сообщение/получатель, lease/token, Resend idempotency key; статусы PENDING/PROCESSING/SENT/FAILED/CANCELED, максимум 5 попыток. SENT = принят провайдером, не подтверждение доставки
- [x] Настройки владельца: включение/отключение, язык писем `notificationLocale` отдельно от публичной формы, адрес аккаунта readonly. Произвольный notificationEmail и сотрудники отложены
- [x] Ежедневный cron 08:00 UTC (`vercel.json`), endpoint `/api/cron/email-notifications`, Bearer `CRON_SECRET`; до 20 заданий/ограничение времени, без быстрого планировщика. Local-задания обрабатываются локальным вызовом endpoint; stage не подбирает их
- [x] Snapshot получателя/отправителя/письма и origin из `NEXT_PUBLIC_APP_URL`: local/stage изолированы даже при общей БД; перед отправкой проверяются роль OWNER, email/подтверждение и включение уведомлений
- [x] Пользователь подтвердил получение уведомления о новом отзыве; строка «Настроение отправителя: текст + эмоджи» работает
- [ ] Полный stage-прогон: RU/EN, выключение уведомлений, повтор формы и ошибки; проверить применение миграции/переменных на production перед запуском
- Ограничение: неоднозначную отправку (timeout/5xx/смерть worker) после 23 часов не повторяем вслепую из-за 24-часового окна идемпотентности Resend. Без webhook нет отслеживания delivered/bounced. Snapshot содержит фрагмент отзыва; политику очистки очереди добавить позже
- Проверки: Prisma validate/generate, TypeScript/ESLint, `node scripts/email-notifications.test.mjs` (шаблоны/экранирование/локаль/retry policy); реальная доставка подтверждена пользователем. SQL-конкуренция и полный stage-прогон отдельно не подтверждены

### 8. PWA

- Manifest, service worker и offline fallback
- Установка и инструкции для iPhone
- Web Push и badge непрочитанных сообщений

### 9. Тарифы

- Подписка относится к организации
- Переключатели организации и объекта в кабинете (отложены из этапа 3)
- Ограничения Free и entitlement-проверки
- Несколько организаций, объектов и QR
- Приглашение сотрудников
- Подключение оплаты позднее
- Платные лимиты проверяются бизнес-логикой, не схемой БД

### 10. VPS-деплой — после успешного запуска MVP

- Dockerfile и Next.js `output: 'standalone'`
- Production image в GitHub Container Registry
- GitHub Actions: build, push image, deploy по SSH
- Caddy/Nginx, HTTPS, healthcheck, restart и rollback
- Миграции `prisma migrate deploy` отдельным шагом деплоя
- Заменить Vercel Cron на cron Ubuntu для обработки очереди email (см. раздел VPS ниже)
- Сначала можно оставить Neon; перенос PostgreSQL сделать отдельно

### 11. Политика и согласие при регистрации — отложено до завершения обкатки

- Оператор: физическое лицо; контакт по персональным данным — `georgyrevazashvili@yandex.ru`. Данные оператора/адрес и сроки хранения уточнить перед публикацией, не придумывать.
- [ ] Проверить правовые основания по законодательству Беларуси: данные аккаунтов, клиентские отзывы, роли сервиса/организации, зарубежные Vercel/Neon/Resend и трансграничная передача; финальный текст согласовать юридически. Отсрочка реализации не означает подтверждение соответствия текущего релиза закону.
- [ ] Создать публичные `/privacy` и `/en/privacy`: цели, состав данных, сроки, получатели, cookies, права, отзыв согласия и порядок удаления. Обновить ссылки в marketing/auth footer и формах; старый URL перенаправить. Юридические страницы пока noindex.
- [ ] Разделить политику, согласие, условия использования и подтверждение email; определить необходимость условий обработки отзывов по поручению организаций и отдельного согласия на трансграничную передачу.
- [ ] Добавить пустую по умолчанию галочку регистрации со ссылками на документы. Использовать Base UI Checkbox: существующий `src/components/ui/checkbox.tsx` сейчас основан на нативном input; адаптировать его с сохранением совместимости либо выделить отдельную Base UI-обёртку после проверки использований.
- [ ] Серверная проверка и запись доказательства согласия (пользователь, дата, цель, версия текста, способ); предусмотреть отзыв. Не объединять согласие на рекламу с обязательными условиями сервиса.
- [ ] Yandex signup: собственные условия/согласие до начала регистрации и серверная фиксация; OAuth-разрешение Яндекса не считать универсальным согласием нашему сервису. Повторный login не требует галочку каждый раз; существующим пользователям предусмотреть отдельный flow, если необходим.
- [ ] Оценить информацию/согласие для публичной формы отзывов и cookies; аналитика пока выключена. Проверить ru/en, email/password и Yandex, обход UI прямым API-запросом и доступность Checkbox.

## Модель

```text
User ↔ Membership ↔ Organization
Organization → FeedbackTarget → FeedbackChannel → Message
Organization → Subscription
User → PushSubscription
```

- Один пользователь может создать несколько организаций.
- `Organization` — владелец объектов, сотрудников, подписки и биллинга.
- `FeedbackTarget` — конкретное место, филиал, товар или услуга, название которых видит клиент.
- В организации несколько `FeedbackTarget`: кафе, филиалов, товаров или услуг.
- У объекта несколько QR-каналов; каждый имеет стабильный случайный `publicId`.

## Основные решения

- Next.js App Router, PostgreSQL/Neon, Prisma, Better Auth, Zod, `next-intl`.
- Чтение: Server Components через DAL. Изменения: Server Actions.
- Route Handlers: auth, публичные API, webhooks и интеграции.
- Проверять сессию, роль и принадлежность организации внутри каждой серверной операции.
- Не привязывать ядро к Vercel Blob/KV/Cron; файлы — через S3-совместимый адаптер.
- Секреты только в `.env`/Vercel; `.env` не коммитить.

## Команды

```bash
nvm use
pnpm install
pnpm dev
pnpm lint
pnpm build

pnpm db:validate
pnpm db:generate
pnpm db:migrate
pnpm db:migrate:deploy
pnpm db:migrate:stage
pnpm db:migrate:prod
pnpm db:studio
pnpm db:studio:stage
pnpm db:studio:prod
```

Node `22.17.0`, pnpm `10.24.0`, Prisma `7.10.0` зафиксированы в проекте.

## Окружение

- `DATABASE_URL` — pooled Neon URL для runtime.
- `DIRECT_URL` — direct Neon URL для Prisma migrations.
- `APP_ENV` — `stage` или `production`; нужен, поскольку оба Vercel-проекта считают свой основной деплой Production.
- `NEXT_PUBLIC_APP_URL` — `http://localhost:3000` локально, публичный URL соответствующего Vercel-проекта после деплоя.
- Локальная разработка использует Neon-ветку `stage`, а не `production`.
- Prisma Studio по окружению: `pnpm db:studio:stage` (localhost:5555) / `pnpm db:studio:prod` (localhost:5556), используют `.env.migrations.stage` / `.env.migrations.prod` и те же проверки, без смены рабочего `.env`. Браузер открыть вручную; `--check` проверяет конфигурацию без запуска Studio/подключения к БД. Правки/удаления в Studio меняют выбранную базу напрямую; окружение и hostname напечатаны перед запуском.
- Применение готовых миграций: скопировать `.env.migrations.example` в `.env.migrations.stage` и `.env.migrations.prod`; указать APP_ENV stage/production и DIRECT_URL соответствующей Neon-ветки. Секретные файлы игнорируются Git. Команды `pnpm db:migrate:stage` / `pnpm db:migrate:prod` запускают только `migrate deploy`, без fallback на рабочий `.env`. `--check` проверяет конфигурацию без подключения к БД; `--status` проверяет историю миграций без применения. Метка APP_ENV проверяется, но принадлежность hostname ветке нужно сверить в Neon. Создание новых миграций — прежний `pnpm db:migrate` на локальной stage-базе.

## Деплой

### Vercel

- `master` → Vercel `backsignal` → Neon `production` → `https://backsignal.tech`.
- `stage` → Vercel `backsignal-stage` → Neon `stage`.
- В каждом Vercel-проекте его рабочая Git-ветка назначена Production Branch.
- Production-переменные каждого проекта указывают только на соответствующую Neon-ветку.
- Не добавлять доступ к production-базе в Preview основного проекта.
- Из-за общего репозитория push в `stage` также запускает ненужный Preview в `backsignal`; отключить перекрёстные сборки через Ignored Build Step.
- Миграции: `pnpm db:migrate:deploy`; не использовать `db push` в production.
- Новые миграции сначала проверять на `stage`, затем применять к `production`.

### VPS

- Отложено до подтверждения жизнеспособности MVP; сейчас Docker не устанавливается.
- План: Docker + Next.js `output: 'standalone'` + image в GHCR + GitHub Actions + Caddy/Nginx.
- Сначала можно перенести приложение, оставив Neon; затем БД через `pg_dump`/`pg_restore`.
- Собственный домен сохраняет работоспособность напечатанных QR после смены хостинга.
- [ ] Настроить cron Ubuntu вместо Vercel Cron: раз в сутки вызывать `/api/cron/email-notifications` через локальный порт приложения с `Authorization: Bearer CRON_SECRET`. API, очередь и отправка через Resend остаются прежними, в том числе при Docker-деплое.
- [ ] Хранить cron-скрипт/секрет в защищённом файле вне репозитория; проверить timezone (целевое время 08:00 UTC / 11:00 Минск), логирование ошибок и запуск после перезагрузки. После переключения отключить старый Vercel Cron, чтобы не оставлять два планировщика.

## Журнал

- 2026-09-18: пользователь подтвердил, что фикс корневого layout устранил полосу над dashboard header на Android. Политика и согласие регистрации вынесены в отложенный раздел 11 после обкатки; предусмотрены Base UI Checkbox, серверная фиксация и Yandex flow. Юридические страницы/галочка пока не реализованы.

- 2026-09-18: для устранения полосы над dashboard header на Android корневой locale-layout изменён: body justify-center → justify-start, min-h-screen → min-h-dvh, согласован с высотой кабинета. Header/скролл не фиксировались, auth сохраняет собственный layout. Причина предположительная; мобильная проверка переходов/loading после деплоя — пользователем.

- 2026-09-18: исправлена отметка READ при открытии отзыва из email (`?message=id`): карточка изначально раскрыта, поэтому onToggle при hydration недостаточен. Добавлена явная отметка после mount через существующий защищённый Server Action с обновлением layout/счётчика. Ref предотвращает параллельные запросы mount/toggle и повторный автоматический запрос при ошибке; ручной retry сохранён. Миграции не нужны; браузерный прогон после деплоя — пользователем.

- 2026-09-18: добавлены db:studio:stage/prod на общих изолированных env-файлах с миграциями, отдельные порты 5555/5556 и --check. Studio и миграции при реализации не запускались; данные не удалялись.

- 2026-09-18: уточнён UI неподтверждённой почты: переключатель уведомлений без галочки и недоступен до verification, рядом кнопка отправки письма. Сохранённое предпочтение не сбрасывается; если включено, пояснение сообщает об автоматической активации после подтверждения. На старом production кнопка появится после деплоя фикса.

- 2026-09-18: исправлено отсутствие email-уведомлений после Yandex signup: провайдер сохраняет emailVerified=false, отправка отзывов требует подтверждения. Включена автоматическая verification после OAuth-регистрации; письмо RU/EN объясняет необходимость подтверждения для уведомлений, не блокируя кабинет. Password signup сохраняет явный запрос из формы без двойного письма. Для существующих пользователей добавлена кнопка отправки verification в настройках уведомлений; callback возвращает в локализованные настройки. База/миграции не менялись. После подтверждения тестировать новым отзывом: пропущенные ранее письма не создаются задним числом. Реальный OAuth/signup/email-прогон после деплоя — пользователем.

- 2026-09-18: пользователь применил все production-миграции; `db:migrate:prod --status` подтвердил актуальную схему. На лендинге демо заменено рабочей ссылкой `/q/d9765fd4-5273-4e65-a829-ef0bbb86154e` и предоставленным SVG QR (с белым quiet zone); копирование использует тот же URL. План запуска: MVP без PWA, PWA следующим обновлением. Сканирование QR на production проверить после деплоя.

- 2026-09-18: добавлены отдельные команды применения миграций stage/prod и изолированные env-файлы; рабочий `.env` менять перед релизом не нужно. Есть проверки окружения/direct URL и режимы --check/--status. Миграции при настройке скриптов не запускались.

- 2026-09-18: в план VPS добавлена замена Vercel Cron на cron Ubuntu с вызовом существующего защищённого API; учтены секрет, timezone, логи и отключение старого расписания. Сейчас инфраструктура не менялась.

- 2026-09-18: индексация разрешена только marketing-лендингу RU/EN при `APP_ENV="production"`; stage/local и неизвестное окружение закрыты. Корневой locale-layout, auth, dashboard и публичные `/q/[publicId]` используют `noindex, nofollow`; дочерние metadata не переопределяют запрет. `NEXT_PUBLIC_ALLOW_INDEXING` больше не используется; после смены APP_ENV нужен новый build/deploy.

- 2026-09-18: пользователь подтвердил работу email-уведомлений после разделения ключей Resend. В HTML/plain text добавлена локализованная строка «Настроение отправителя: Положительное 🙂 / Нейтральное 😐 / Отрицательное 😞», без внешних картинок; пользователь проверил. Тесты шаблонов и ESLint прошли. Следующий этап MVP — PWA; полный stage/production-прогон остаётся перед запуском.

- 2026-09-18: исправлен устаревающий счётчик NEW: общий layout сохраняется при навигации, поэтому добавлен `router.refresh()` после смены раздела, при возврате во вкладку/окно и повторном клике «Сообщения»/мобильный конверт. Список и счётчики обновляются из одного серверного дерева; без state manager и polling. Префикс `[STAGE]` убран из новых email-уведомлений; существующие snapshots в очереди не изменяются.

- 2026-09-18: после stage/local ошибки Resend 403 разделены ключи: `RESEND_API_KEY` только для auth, `RESEND_NOTIFICATION_API_KEY` для notify.backsignal.tech (отдельные stage/production, local использует stage). Уведомления не используют auth-key как fallback. Добавлен стабильный код ошибки, если Resend не возвращает name. Старое задание после 403 остаётся FAILED; для проверки после смены ключа отправить новый отзыв.

- 2026-09-18: реализованы email-уведомления об отзыве через отдельный `notify.backsignal.tech`: атомарная очередь PostgreSQL, RU/EN шаблоны, первая попытка `after()`, Resend deduplication/lease/безопасные повторы и ежедневный защищённый cron. Добавлены настройки владельца и язык писем, ссылки на конкретный отзыв с проверкой доступа. Подготовлена миграция `20260918090000_email_notifications`, база не изменялась; Prisma Client сгенерирован. Требуются `CRON_SECRET` и корректный `NEXT_PUBLIC_APP_URL` по окружениям, stage-проверка пользователем до production. На VPS тот же worker вызывать cron без Vercel.

- 2026-09-17: добавлен QR-конструктор: браузерный PDF через `@react-pdf/renderer`, локальный Inter, векторные QR и RU/EN логотипы, название/призыв, независимый язык макета, предпросмотр и скачивание без сохранения настроек. Форматы A4/A5/A6/евро/карточка 90×50 и «Пирамидка» A4 с 3 гранями, клапаном и линиями сгиба. В aside после сообщений — «Создать макет», затем «Только QR-код», затем настройки; SVG/PNG 2048 также доступны в настройках/онбординге. Общий Base UI Select; ширина popup привязана к trigger. Playwright/Chromium отложен как вариант для VPS. Пользователь проверил текущую реализацию. Пропадание первой буквы английского призыва не воспроизведено; сейчас пользователь подтверждает корректный текст, при повторении нужен проблемный PDF. Проверки: TypeScript, ESLint, 14 PDF-тестов; физическая печать/сборка и полный mobile/stage-прогон ещё не подтверждены.

- 2026-09-17: уточнён UI сообщений: смайлики с обводкой, превью одной строкой (`truncate`, переносы заменяются пробелами), полный текст с абзацами при раскрытии. Вместо даты — локализованное время «назад» RU/EN с обновлением раз в минуту; точная дата UTC в title. Исправлена необязательная оценка публичной формы: RHF возвращает null без выбора, общая клиентская/серверная схема преобразует null/undefined в NEUTRAL; добавлен регрессионный тест. TypeScript, ESLint и тест валидации прошли; миграция не нужна.

- 2026-09-17: карточки сообщений приведены к референсу: общий MoodIcon с публичной формой, без видимых текстовых статусов/названия объекта; NEW подсвечены, READ белые. Добавлены общий счётчик NEW в desktop/mobile-навигации и списке, Server Action отметки READ/readAt при раскрытии с проверкой доступа в DAL, идемпотентностью и обработкой ошибок. Миграция не нужна. Отображение источника отложено до платного мультитенантного UI.

- 2026-09-17: пользователь подтвердил применение миграции и сохранение публичных сообщений в БД. Dashboard переключён с MessagesPreview на реальные данные: серверная сессия + организация + проверка членства в DAL, новые сверху, текст/настроение/статус/дата UTC, раскрытие полного текста, пагинация 30, кнопка обновления. Изменение статуса и realtime не добавлялись; проверка отображения — пользователем.

- 2026-09-17: локальные проверки публичной формы прошли: Prisma validate/generate, Next typegen, TypeScript, ESLint, unit-тесты `node --experimental-strip-types --test scripts/public-feedback.test.mjs scripts/onboarding.test.mjs`. Сохранение в БД, SQL-лимиты и браузерный вид ждут stage-проверки после миграции; production не изменялся.

- 2026-09-17: реализована публичная mobile-first форма без CAPTCHA: название, textarea, radio-настроение с default NEUTRAL, RU/EN, благодарность. Добавлены honeypot, PostgreSQL rate limit (30 попыток/IP и 3 сообщения/IP/канал за fixed window 10 минут), HMAC-IP, уникальный submissionId и транзакционная защита дублей. Подготовлена миграция поля настроения/идентификатора/счётчиков; Prisma Client обновлён, база не изменялась. Миграция и интеграционная проверка — пользователем. Уведомления и реальные сообщения в кабинете ещё не подключены.

- 2026-09-16: план публичной формы уточнён: mobile-first по референсу, три настроения с серверным default NEUTRAL, миграция поля Message; PostgreSQL rate limit + honeypot + защита от дублей, CAPTCHA-провайдер выбирается отдельно. Реализация этапа 5 ещё не начата.

- 2026-09-16: пользователь подтвердил корректную работу онбординга после исправлений: локаль передаётся в Server Action явно; QR отображается квадратным, PNG сохраняется 512×512. Этап 4 завершён. Статус этапа 0 обновлён на «завершено»; VPS/Docker остаются отложенными.

- 2026-09-16: реализован обязательный onboarding с одним названием, атомарным созданием Organization + Membership OWNER + FeedbackTarget + FeedbackChannel, настройками языка и Free-подпиской. Добавлены персональная ссылка, локальная генерация QR/скачивание PNG (`qrcode`), повторный доступ в настройках. База и миграции не запускались; интеграционная проверка на stage — пользователем. Локальные проверки: `pnpm exec tsc --noEmit`, `pnpm lint`, `node --experimental-strip-types --test scripts/onboarding.test.mjs`.

- 2026-09-16: пользователь проверил меню профиля на desktop и mobile. На desktop — dropdown с подменю языков; на mobile — шторка из header/aside и вложенная шторка языков с backdrop, границей и закрытием по тапу вне неё. TypeScript и ESLint прошли.
- 2026-09-16: создан каркас кабинета: защищённый layout, sticky aside, мобильный Dialog, страницы `/dashboard`, `/dashboard/settings`, `/dashboard/settings/profile`, локализация ru/en, skeleton/error/empty состояния и примеры сообщений. TypeScript и ESLint прошли; гостевые ru/en-редиректы проверены. Настройки не сохраняются, onboarding и реальные сообщения пока не подключены.
- 2026-09-16: реализован вход через Yandex ID: Generic OAuth, общая кнопка login/signup с `ya.svg`, локализованные callbacks и ошибки. Linking разрешён только с подтверждённым локальным аккаунтом; разные email, перезапись профиля и отключение последнего способа входа запрещены. Миграция не требуется; UI управления способами входа отложен до настроек профиля.
- 2026-09-15: порядок MVP скорректирован: после авторизации сначала создаётся каркас кабинета, затем обязательный онбординг в блокирующем Dialog. Онбординг атомарно создаёт организацию, владельца, первый объект и QR-канал; отдельный флаг завершения не используется.
- 2026-09-15: завершён базовый auth-flow: регистрация, подтверждение email, вход, выход, сессии, защита кабинета и восстановление пароля. Почтовый слой через Resend готов к повторному использованию для уведомлений о новых сообщениях; в этапе авторизации остаются Yandex OAuth и account linking.
- 2026-09-15: Resend проверен доставкой на Yandex; включено обязательное подтверждение email, повторная отправка при входе и восстановление пароля с локализованными письмами.
- 2026-09-15: базовая email/password-авторизация проверена полностью; добавлены route groups, защищённый dashboard и выход. Для auth-писем настроен домен `auth.backsignal.tech`, добавлен адаптер Resend.
- 2026-09-13: завершена интернационализация лендинга: `next-intl`, словари `ru/en`, locale routing, локализованные metadata и навигация, desktop/mobile переключатель языка на Base UI.
- 2026-09-12: восстановлен отдельный этап интернационализации перед авторизацией; этапы перенумерованы.
- 2026-09-12: проверено разделение stage/production; перекрёстные Preview-сборки отключены.
- 2026-09-12: созданы отдельные Neon/Vercel-окружения `stage` и `production`; зафиксирована необходимость отключить перекрёстные Preview-сборки.
- 2026-09-12: Docker и автоматический VPS-деплой вынесены из фундамента в отложенный этап после запуска MVP.
- 2026-09-12: создан DAL для организаций, объектов и сообщений с проверкой членства и ролей.
- 2026-09-12: создана и применена первая миграция `20260911213810_init` с мультитенантной доменной схемой.
- 2026-09-12: подключение к Neon подтверждено через Prisma (`SELECT 1`); VPN блокировал доступ к endpoint.
- 2026-09-12: зафиксированы Node 22.17, Prisma 7.10; добавлена конфигурация Neon/Prisma и env-шаблон.
